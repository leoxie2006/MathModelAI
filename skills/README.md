# Skills 示例

每个技能包是一个目录，目录内必须包含 `SKILL.md`。运行时会按 `name` 和 `description` 渐进披露，模型需要时再加载全文。

当前保留的数学建模示例：

- `problem-formulation`：问题抽象、变量定义与假设管理。
- `model-validation`：模型验证、敏感性分析与误差检查。
- `team-collaboration-workflow`：三人小队协作协议、交接卡、会议和 checkpoint。
