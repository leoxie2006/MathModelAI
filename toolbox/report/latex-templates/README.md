# LaTeX 模板目录

这里存放由官方 Word 模板转换得到的 LaTeX 模板。

## 预期文件

- `main.tex`：主入口模板。
- `mathmodel.cls` 或 `mathmodel.sty`：版式复刻层。
- `references.bib`：参考文献库。
- `figures/`：图表目录。

LaTeX 模板应优先复刻官方 Word 模板版式，而不是套用通用论文样式。
